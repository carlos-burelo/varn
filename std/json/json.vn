import { parse$, stringify$ } from "runtime:json";

export type Json = int | float | str | bool | null | Json[] | { [key: str]: Json };

export namespace JSON {
    export function parse<T = Json>(text: str): T {
        return parse$(text) as T;
    }

    export function tryParse<T = Json>(text: str): T? {
        try {
            return parse$(text) as T;
        } catch {
            return null;
        }
    }

    export function stringify(value: dynamic, space?: int): str {
        return stringify$(value);
    }
}
