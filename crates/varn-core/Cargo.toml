[package]
name = "varn-core"
version = "0.1.0"
edition = "2021"

[dependencies]
varn-diagnostics = { path = "../varn-diagnostics" }
varn-base = { path = "../varn-base" }
serde = { version = "1", features = ["derive", "rc"] }
rustc-hash = "1.1.0"

[features]
default = []
