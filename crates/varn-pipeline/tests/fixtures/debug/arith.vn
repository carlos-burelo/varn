function add(a: int, b: int): int {
    return a + b
}

function scale(x: int): int {
    return x * 2 + 1
}

const r = add(1, 2)
assert("arith add", r === 3)
assert("arith scale", scale(20) === 41)
print("arith ok")
