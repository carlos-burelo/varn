import { ioWrite, ioWriteln, ioFlush, ioReadLine } from "runtime:io";

export interface Writer {
    write(content: str): void
}

export interface Reader {
    read(size?: int): str
    readAll(): str
    pipeTo(dst: Writer): void
}

export namespace io {
    export function write(...args: dynamic[]): void {
        ioWrite(...args);
    }

    export function writeln(...args: dynamic[]): void {
        ioWriteln(...args);
    }

    export function print(...args: dynamic[]): void {
        ioWriteln(...args);
    }

    export function flush(): void {
        ioFlush();
    }

    export function readLine(prompt?: str): str {
        if (prompt === null) {
            return ioReadLine("");
        }
        return ioReadLine(prompt);
    }

    export function readAll(): str {
        return "";
    }
}
