export declare class bigint {
    toString(): str;
    toStr(): str;
    toInt(): int;
    toFloat(): float;
}

export declare class Range {
    readonly start: int;
    readonly end: int;
    readonly inclusive: bool;
    constructor(start: int, end: int, inclusive: bool);
    static from(start: int, end: int): Range;
    static fromInclusive(start: int, end: int): Range;
    contains(val: int): bool;
    toArray(): int[];
    step(s: int): int[];
    forEach(callback: (val: int) => void): void;
    map<U>(callback: (val: int) => U): U[];
    toString(): str;
}
