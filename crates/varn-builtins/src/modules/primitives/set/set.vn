export declare class Set<T> {
    constructor();
    @fallible add(value: T): void;
    @fallible has(value: T): bool;
    @fallible delete(value: T): bool;
    clear(): void;
    values(): T[];
    @fallible forEach(callback: (value: T, value2: T, s: Set<T>) => void): void;
    readonly size: int;
}