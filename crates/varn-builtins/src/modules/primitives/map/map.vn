export declare class Map<K, V> {
    constructor();
    @fallible get(key: K): V?;
    @fallible set(key: K, value: V): void;
    @fallible has(key: K): bool;
    @fallible delete(key: K): bool;
    clear(): void;
    keys(): K[];
    values(): V[];
    entries(): [K, V][];
    @fallible forEach(callback: (value: V, key: K, map: Map<K, V>) => void): void;
    readonly size: int;
}