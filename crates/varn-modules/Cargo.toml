[package]
name = "varn-modules"
version = "0.1.0"
edition = "2021"

[dependencies]
varn-core = { path = "../varn-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
semver = "1"
