function loop_add(limit: int): int {
    let sum = 0
    let i = 0
    while (i < limit) {
        sum = sum + i
        i = i + 1
    }
    return sum
}

// Execute the loop with a large limit to see the difference
const res = loop_add(100000)
