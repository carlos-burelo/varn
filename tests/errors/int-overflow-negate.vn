// expect: error[integer overflow]
//
// -INT_MIN has no i48 representation. Unchecked negation returned INT_MIN
// again, so `-x === x` held for one value of `x`.
function neg(a: int): int { return -a }
print(neg(-140737488355328))
