// expect: error[integer overflow]
//
// `-INT_MIN` has no `int` representation: the two's-complement range is
// asymmetric. Unchecked negation returned INT_MIN again, so `-x === x` held
// for one value of `x`.
function neg(a: int): int { return -a }
print(neg(-9223372036854775807 - 1))
