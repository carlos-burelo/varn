// expect: warning[VN4001]
enum Color {
    Red,
    Green,
    Blue,
}

function describe(c: Color): str {
    return match (c) {
        Color.Red => "rojo",
        Color.Green => "verde",
    }
}
