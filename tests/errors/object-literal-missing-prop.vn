// expect: error[VN3001]
// El literal cumple la propiedad de array pero omite `name`, que es requerida:
// aceptarlo dejaria un campo tipado `str` con null dentro.
let o: { xs: Array<float>, name: str } = { xs: [1, 2] }
print(o.name)
