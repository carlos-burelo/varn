// expect: error[integer overflow]
//
// `int` is a native i64 and raises rather than wrapping.
//
// Uncaught, it aborts the run; caught, it is an `Error` (see `tests/53-int-overflow.vn`).
function mul(a: int, b: int): int { return a * b }
print(mul(4000000000, 4000000000))
